const PendingBtn = () => {
  return (
    <button className="rounded-[60px] bg-fillOne p-[5px] sm:px-[16px] sm:py-[10px] border border-strokeOne text-[12px] sm:text-[16px] text-textTwo flex items-center gap-[15px] hover:cursor-wait">
      <div>Pending</div>
    </button>
  );
};

export default PendingBtn;
